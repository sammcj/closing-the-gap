const config = {
  predictionMonths: 6,
  trendDataPoints: 12,
  openColor: 'rgb(75, 192, 192)',
  closedColor: 'rgb(255, 99, 132)',
  legendPosition: 'top',
  chartTitle: 'LLM Benchmark Progress',
  enableDataEntry: true,
  enableAdminPanel: true  // Set this to false to disable admin functions
};

export default config;
