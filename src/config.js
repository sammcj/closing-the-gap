const config = {
  predictionMonths: 12,
  trendDataPoints: 6, // Number of recent data points to use for trend calculation
  openColor: 'rgb(75, 192, 192)',
  closedColor: 'rgb(255, 99, 132)',
  legendPosition: 'top',
  chartTitle: 'LLM Benchmark Progress',
  yAxisMin: 0.5,
  yAxisMax: 1
};

export default config;
